import styled from "styled-components";

export const MainBills = styled.div`
  padding: 12px;
  background-color: ${(P) => P.them.backgroundColor};
  border-radius: 5px;
  color: ${(P) => P.them.color};
  box-shadow: #030303e0 0px 0px 11px 0px;


`;
